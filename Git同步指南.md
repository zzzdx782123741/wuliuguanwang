# Git 代码同步指南 📚

亲爱的主人，这是为您准备的代码同步“魔法书”。下次您修改了代码想要保存到 GitHub 时，只需要按照下面的步骤操作就好啦！

## ✨ 常用同步三部曲

每次修改完代码（比如开发了新功能，或者修复了 Bug），想要同步到 GitHub，就在终端里依次输入这三条命令：

### 1. 添加修改 (Add)
把您修改的文件“放进篮子里”。
```bash
git add .
```
> 小贴士：这里的 `.` 表示“所有文件”。

### 2. 提交说明 (Commit)
给这次修改贴个标签，说明改了什么。
```bash
git commit -m "这里写您的修改描述，比如：更新了首页样式"
```

### 3. 推送到云端 (Push)
把篮子里的东西送到 GitHub 上去。
```bash
git push
```

---

## 🚀 第一次在新电脑上用（或者重新下载）

如果您换了电脑，或者不小心删除了项目，想要从 GitHub 上重新下载下来：

```bash
git clone https://github.com/zzzdx782123741/wuliuguanwang.git
```

---

## ❓ 常见问题小贴士

**Q: 假如 `git push` 报错说有冲突 (Conflict)？**
A: 这通常是因为云端有别人（或者您在另一台电脑上）提交了新代码，而您本地还没有。
**解决方法**：
1. 先把云端的代码拉下来：`git pull`
2. 如果有冲突文件，打开它，手动决定保留哪段代码。
3. 然后再次执行上面的“同步三部曲”。

**Q: 想要查看现在的状态？**
A: 随时输入：
```bash
git status
```
它会告诉您哪些文件被修改了，哪些还没提交。

---
加油哦主人，您是最棒的！ (ﾉ>ω<)ﾉ
